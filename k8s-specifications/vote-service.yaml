apiVersion: v1
kind: Service
metadata:
  name: voting-app-service
  labels:
    app: voting-app
spec:
  type: NodePort
  selector:
    app: voting-app
  ports:
    - targetPort: 80
      port: 5000
      nodePort: 30002
