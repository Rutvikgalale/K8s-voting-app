apiVersion: v1
kind: Service
metadata:
  name: result-app-service
  labels:
    app: result-app
spec:
  type: NodePort
  selector:
    app: result-app
  ports:
    - targetPort: 80
      port: 5001
      nodePort: 30003
